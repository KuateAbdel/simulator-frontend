# URL du backend Loader (API + Swagger).
#
# DEV LOCAL : laisser VIDE → les appels restent same-origin et le proxy de
#   vite.config.ts les relaie vers simul.api.fintech4esg.com (le CORS du
#   backend n'autorise que l'origine de production, jamais localhost).
# BUILD DE PROD : https://simul.api.fintech4esg.com (l'origine
#   https://simul.fintech4esg.com est autorisee par le CORS du backend).
VITE_API_URL=
